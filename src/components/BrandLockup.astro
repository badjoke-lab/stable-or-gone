---
import { uiV2BrandAssets } from '../../config/ui-v2-foundation.mjs';

interface Props {
  compact?: boolean;
  surface?: 'dark' | 'light';
  href?: string;
  class?: string;
}

const {
  compact = false,
  surface = 'dark',
  href = '/',
  class: className = ''
} = Astro.props;
const asset = compact
  ? (surface === 'dark' ? uiV2BrandAssets.mark_on_dark : uiV2BrandAssets.mark_on_light)
  : (surface === 'dark' ? uiV2BrandAssets.lockup_on_dark : uiV2BrandAssets.lockup_on_light);
---
<a class:list={['brand-lockup', compact && 'brand-lockup--compact', className]} href={href} aria-label="Stable or Gone home">
  <img src={asset} alt="Stable or Gone" width={compact ? 56 : 228} height={compact ? 56 : 56} decoding="async" />
</a>
