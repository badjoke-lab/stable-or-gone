name: PR 374 Planning Queue Review-History Contract

on:
  push:
    branches:
      - pr374-planning-queue-review-history-contract
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  validate:
    if: github.event_name != 'pull_request' || github.head_ref == 'pr374-planning-queue-review-history-contract'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22.22.0
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Generate deterministic review-history outputs
        run: |
          node scripts/build-planning-queue-review-history-pr374.mjs 2>&1 | tee /tmp/pr374-generation.txt
          node scripts/build-planning-queue-review-history-pr374.mjs --check
      - name: Validate review-history contract
        run: |
          node scripts/validate-planning-queue-review-history-pr374.mjs 2>&1 | tee /tmp/pr374-validation.txt
          npm run validate:active-workstream
      - name: Validate existing canonical contracts
        run: |
          npm run validate:data
          npm run validate:compat
          npm run validate:final
          npm run validate:release-integrity
      - name: Verify internal-planning-only boundary
        run: |
          git diff --exit-code origin/main...HEAD -- data/ src/ public/
          test ! -e public/data/planning-queue-review-history-manifest-pr374.json
          test ! -e public/data/planning-queue-review-history-audit-pr374.json
          test ! -e dist/data/planning-queue-review-history-manifest-pr374.json
          test ! -e dist/data/planning-queue-review-history-audit-pr374.json
      - name: Astro check and build
        run: |
          npm run check
          npm run build
      - name: Commit generated review-history outputs
        if: github.event_name == 'push'
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            docs/migration/planning-queue-review-history-manifest-pr374.json \
            docs/migration/planning-queue-review-history-audit-pr374.json
          if git diff --cached --quiet; then
            echo 'Review-history outputs already committed'
          else
            git commit -m 'Generate PR 374 planning queue review-history outputs'
            git push origin HEAD:pr374-planning-queue-review-history-contract
          fi
      - name: Publish PR 374 status
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const marker='<!-- pr374-planning-queue-review-history-status -->';
            const fs=require('fs');
            let manifest=null,audit=null;
            try{manifest=JSON.parse(fs.readFileSync('docs/migration/planning-queue-review-history-manifest-pr374.json','utf8'));}catch{}
            try{audit=JSON.parse(fs.readFileSync('docs/migration/planning-queue-review-history-audit-pr374.json','utf8'));}catch{}
            let diagnostics='';
            try{diagnostics=fs.readFileSync('/tmp/pr374-validation.txt','utf8').trim();}catch{}
            const body=`${marker}
            ## PR #374 Planning Queue Review-History Contract

            - Workflow: **${{ job.status }}**
            - Sources / events / assets: **${manifest?.counts?.history_source_count??'n/a'} / ${manifest?.counts?.history_event_count??'n/a'} / ${manifest?.counts?.reviewed_asset_count??'n/a'}**
            - Effective dimensions complete / partial / no-safe-change: **${manifest?.counts?.effective_reviewed_complete_count??'n/a'} / ${manifest?.counts?.effective_reviewed_partial_count??'n/a'} / ${manifest?.counts?.effective_reviewed_no_safe_change_count??'n/a'}**
            - Current queue / fully suppressed: **${audit?.current_queue_audit?.source_candidate_count??'n/a'} / ${audit?.current_queue_audit?.fully_suppressed_candidate_count??'n/a'}**
            - Projected v2.2 queue: **${audit?.current_queue_audit?.projected_v2_2_candidate_count??'n/a'}**
            - Next work item: **${audit?.decision?.next_work_item??'n/a'}**
            - Head: \`${context.sha}\`

            <details><summary>Validation diagnostics</summary>

            \`\`\`text
            ${diagnostics.slice(0,12000)}
            \`\`\`
            </details>
            `;
            const comments=await github.paginate(github.rest.issues.listComments,{owner:context.repo.owner,repo:context.repo.repo,issue_number:context.issue.number,per_page:100});
            const existing=comments.find((row)=>row.body?.includes(marker));
            if(existing)await github.rest.issues.updateComment({owner:context.repo.owner,repo:context.repo.repo,comment_id:existing.id,body});
            else await github.rest.issues.createComment({owner:context.repo.owner,repo:context.repo.repo,issue_number:context.issue.number,body});
