name: Finalize PR508 terminal-date authority
run-name: Finalize PR508 authority · ${{ github.event_name }} · ${{ github.sha }}

on:
  push:
    branches:
      - agent/terminal-date-boundary-review-batch-1-authority
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: finalize-pr508-terminal-date-authority
  cancel-in-progress: false

jobs:
  finalize:
    if: github.event_name != 'pull_request' || github.head_ref == 'agent/terminal-date-boundary-review-batch-1-authority'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: agent/terminal-date-boundary-review-batch-1-authority

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.22.0

      - name: Generate authority files
        run: node scripts/finalize-pr508-terminal-date-authority.mjs

      - name: Validate authority
        run: node scripts/validate-terminal-date-boundary-review-pr508.mjs

      - name: Restore permanent workflow and remove finalizer
        run: |
          git fetch origin main
          git checkout origin/main -- .github/workflows/pr466-post-ui-data-growth-review-gate.yml
          rm scripts/finalize-pr508-terminal-date-authority.mjs
          rm -f .github/pr508-finalizer-trigger.txt

      - name: Commit authority
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Authorize terminal date boundary review batch 1'
          git push origin HEAD:agent/terminal-date-boundary-review-batch-1-authority
