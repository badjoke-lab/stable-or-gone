name: PR 364 Evidence Duplicate Diagnostic

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  diagnose:
    if: github.event_name == 'workflow_dispatch' || github.head_ref == 'pr364-tier-a-dossier-batch-4'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: pr364-tier-a-dossier-batch-4
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.22.0
          cache: npm

      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund

      - name: Generate Evidence audit and compact finding
        shell: bash
        run: |
          set +e
          node scripts/audit-registry-evidence-integrity.mjs
          set -e
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const report=JSON.parse(fs.readFileSync('data/generated/registry-evidence-integrity-audit.json','utf8'));
          const compact={
            critical:report.findings?.critical??[],
            exact_duplicate_url_groups:report.identity?.exact_duplicate_url_groups??[],
            normalized_only_duplicate_url_groups:report.identity?.normalized_only_duplicate_url_groups??[]
          };
          fs.writeFileSync('docs/migration/pr364-evidence-duplicate-diagnostic.json',`${JSON.stringify(compact,null,2)}\n`);
          NODE

      - name: Commit compact finding
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/migration/pr364-evidence-duplicate-diagnostic.json
          if git diff --cached --quiet; then
            echo 'Exact duplicate diagnostic already synchronized.'
            exit 0
          fi
          git commit -m 'Record exact PR 364 Evidence duplicate findings'
          git pull --rebase origin pr364-tier-a-dossier-batch-4
          git push origin HEAD:pr364-tier-a-dossier-batch-4

      - name: Publish exact duplicate findings
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs=require('fs');
            const marker='<!-- pr364-evidence-duplicate-diagnostic -->';
            let report=null;
            try{report=JSON.parse(fs.readFileSync('docs/migration/pr364-evidence-duplicate-diagnostic.json','utf8'));}catch{}
            const exact=report?.exact_duplicate_url_groups??[];
            const normalized=report?.normalized_only_duplicate_url_groups??[];
            const critical=report?.critical??[];
            const body=`${marker}\n## PR #364 exact Evidence duplicate diagnostic\n\n- Critical findings: **${critical.length}**\n- Exact duplicate URL groups: **${exact.length}**\n- Normalized-only duplicate groups: **${normalized.length}**\n\n\`\`\`json\n${JSON.stringify({critical,exact,normalized},null,2)}\n\`\`\`\n`;
            const comments=await github.paginate(github.rest.issues.listComments,{owner:context.repo.owner,repo:context.repo.repo,issue_number:context.issue.number,per_page:100});
            const existing=comments.find((comment)=>comment.body?.includes(marker));
            if(existing) await github.rest.issues.updateComment({owner:context.repo.owner,repo:context.repo.repo,comment_id:existing.id,body});
            else await github.rest.issues.createComment({owner:context.repo.owner,repo:context.repo.repo,issue_number:context.issue.number,body});
