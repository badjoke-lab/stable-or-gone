name: Support page visual audit

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'scripts/capture-support-visual-review.mjs'
      - '.github/workflows/support-page-visual-audit.yml'
      - 'package.json'
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'scripts/capture-support-visual-review.mjs'
      - '.github/workflows/support-page-visual-audit.yml'
      - 'package.json'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sog-support-visual-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies and Chromium
        run: |
          npm ci --no-audit --no-fund
          npx playwright install --with-deps chromium

      - name: Build static site
        run: npm run build

      - name: Start local site
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 --directory dist > /tmp/sog-support-server.log 2>&1 &
          echo $! > /tmp/sog-support-server.pid
          for attempt in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:4173/ > /dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/sog-support-server.log
          exit 1

      - name: Capture representative support states
        env:
          CAPTURE_BASE_URL: http://127.0.0.1:4173
        run: node scripts/capture-support-visual-review.mjs

      - name: Write review summary
        if: always()
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs'
          const manifestPath = 'artifacts/support-visual-review/manifest.json'
          const lines = ['## SOG support visual review', '']
          if (!fs.existsSync(manifestPath)) {
            lines.push('- Manifest: missing')
          } else {
            const manifest = JSON.parse(fs.readFileSync(manifestPath, 'utf8'))
            lines.push(`- States: ${manifest.captured_state_count}/${manifest.expected_state_count}`)
            lines.push(`- Screenshots: ${manifest.screenshot_count}`)
            lines.push(`- Failures: ${manifest.failure_count}`)
            for (const record of manifest.records) {
              lines.push(`- ${record.page_id} / ${record.device}: support-like links ${record.metrics.support_link_count}, initial viewport ${record.metrics.support_links_in_initial_viewport}`)
            }
          }
          fs.appendFileSync(process.env.GITHUB_STEP_SUMMARY, `${lines.join('\n')}\n`)
          NODE

      - name: Upload support visual review
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sog-support-page-screenshots-${{ github.run_id }}
          path: |
            artifacts/support-visual-review/
            /tmp/sog-support-server.log
          if-no-files-found: error
          retention-days: 30
          compression-level: 0

      - name: Stop local site
        if: always()
        run: |
          if test -f /tmp/sog-support-server.pid; then
            kill "$(cat /tmp/sog-support-server.pid)" || true
          fi
