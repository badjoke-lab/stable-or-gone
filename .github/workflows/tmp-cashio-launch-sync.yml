name: Temporary Cashio launch synchronization

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  sync:
    if: ${{ github.head_ref == 'resolve-cashio-dollar-launch' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: resolve-cashio-dollar-launch
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: node scripts/tmp-sync-cashio-launch.mjs
      - name: Validate full repository
        run: npm run build
      - name: Commit synchronized files
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md data docs
          git commit -m "Resolve Cashio Dollar launch boundary"
          git push origin HEAD:resolve-cashio-dollar-launch
