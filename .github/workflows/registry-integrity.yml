name: Registry integrity

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Rebuild integrity audit
        run: node scripts/audit-registry-integrity-batch-t.mjs
      - name: Validate generated integrity audit
        if: success()
        run: node scripts/audit-registry-integrity-batch-t.mjs --check
      - name: Upload integrity audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: registry-integrity
          path: |
            data/generated/registry-integrity-audit.json
            docs/audits/registry-70-final-audit.md
