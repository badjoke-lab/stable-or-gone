# Production Deployment Log

Production deployments are executed by `.github/workflows/deploy-production.yml`.

The workflow records each run in the repository's dedicated deployment-log issue with:

- source commit;
- trigger type;
- workflow run URL;
- final job result.

The public origin is `https://sog.badjoke-lab.com/`.
