# Organization UI Repair Validation

PR #198 final validation note.

The organization index preserves the protected `Record confidence` desktop column. Public source identity counts remain available in compact cards and organization detail evidence sections.

Relationship history uses canonical `ended` plus `end_date` for classification. The user-facing historical label remains descriptive copy only.
