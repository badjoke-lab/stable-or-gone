# UI v2 Events implementation audit

Date: 2026-06-28  
Program: Modern Data Product UI v2  
Plan item: PR #213 — Events index and detail

The Events index and all event detail routes now use the approved v2 hero, canonical metric hierarchy, and record navigation while preserving the existing five filters, four sorts, URL/history interaction, protected desktop table, compact cards, structured event details, subject relationships, evidence identities, evidence relations, value states, and correction paths.

Canonical counts, routes, and production state are unchanged. No live market data, rankings, synthetic scores, account features, or external runtime fetching were added.

`scripts/validate-ui-v2-events.mjs` is chained into the existing full/site v2 validation entry point.

Deployment classification: No production deployment required.
